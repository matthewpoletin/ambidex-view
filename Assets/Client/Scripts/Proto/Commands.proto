syntax = "proto3";

option csharp_namespace = "Client.Scripts.Generated";

import "Utils.proto";

package Ambidex.View;

message HelloMessage {
    string text = 1;
}

enum JointType {
    UNDEFINED = 0;
    ROTARY = 1;
}

message RobotConfiguration {
    UUID id = 1;
    string configuration_name = 2;
    repeated HelloMessage message = 3;
}

service ModelService {
    rpc SayHello (Empty) returns (HelloMessage) {
    }
}

